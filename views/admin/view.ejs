<%-include('../header.ejs') %>

<section class="dashboard-wrap mtb-40">
  <div class="container">
    <div class="row">
      <div class="col-md-12">
        <div class="dash-right">
          <div class="dash-header">
            <div class="dash-title">
              <h1>Blog View</h1>
            </div>
          </div>
          <table class="table table-bordered table-striped">
            <tr>
              <th>Image</th>
              <th>Title</th>
              <th>Description</th>
            </tr>
            <tr>
              <td><img src="<%= v.image.url %>" width="100px" alt=""></td>
              <td><%= v.title %></td>
              <td><%= v.des%></td>
              
            </tr>
            
          </table>
        </div>
      </div>
    </div>
  </div>
</section>
 

<%-include('../footer.ejs') %>
